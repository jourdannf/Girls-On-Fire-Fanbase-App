import './animation.css'

export default function WinnersTab () {
    //hidden class when it loads and when it's already open
    //open class when it's hidden

    return (
        <div>
            <h1>This is the Winners Tab</h1>
        </div>
    );
}